import { styled } from "@mui/material";
import { NavLink } from "react-router-dom";

export const StyledNavLinkL = styled(NavLink)({
    marginLeft: "400px",
    marginTop: "20px",
    textDecoration: "none",
});